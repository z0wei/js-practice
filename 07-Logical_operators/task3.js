// Вариант 1 — с !
let age = 111;

if (!(age >= 14 && age <= 90)) {
    console.log('Не в диапазоне (вариант с !)');
}


// Вариант 2 — без !
let age2 = 111;

if (age2 < 14 || age2 > 90) {
    console.log('Не в диапазоне (вариант без !)');
}